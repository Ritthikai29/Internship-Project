export interface TopicRejectApproveVendorProject {
    id: number;
    reject_topic: string;
}

export interface RejectApproveVendorProjectinterface {
    reject_topic_id: number | string;
    reject_detail : number | string;
    avp_id: number | string;
}